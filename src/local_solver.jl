"""
Functions that perform local solve of the continuous relaxation of the MINLP.
We remark that at no point in the algorithm are the full MINLP ever solved to local optimality.
"""

function local_solve!(model::MOI.AbstractOptimizer; presolve=false)

    return

end